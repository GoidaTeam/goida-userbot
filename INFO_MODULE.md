# 📊 Модуль Info

Модуль для отображения информации о юзерботе Forelka с превью изображения.

## 🎯 Команда

```
.info - Показать информацию о юзерботе с превью
```

## 📋 Отображаемая информация

- 🖼 **Изображение** - Превью (если установлено)
- 🔥 **Название** - Forelka Userbot
- 👤 **Владелец** - Имя и фамилия из профиля Telegram
- 🌿 **Branch** - Текущая ветка Git
- ⚙️ **Prefix** - Текущий префикс команд
- ⏱ **Uptime** - Время работы юзербота с момента запуска
- 💾 **RAM usage** - Использование оперативной памяти (требуется psutil)
- 🖥 **Host** - Имя хоста/сервера

## 🖼 Изменение изображения

Изображение жестко закодировано в модуле. Чтобы изменить его:

### Способ 1: Редактирование модуля

1. Откройте файл `modules/info.py`
2. Найдите строку (примерно 14-я):
   ```python
   IMAGE_URL = "https://te.legra.ph/file/b8c6c51c36b3c8e8c8c8c.jpg"
   ```
3. Замените на свою ссылку:
   ```python
   IMAGE_URL = "https://ваша-ссылка-на-изображение.jpg"
   ```
4. Перезапустите юзербот

### Способ 2: Загрузка на Telegraph

1. Зайдите на https://telegra.ph
2. Создайте статью
3. Загрузите изображение
4. Скопируйте ссылку на изображение (должна начинаться с `https://te.legra.ph/file/...`)
5. Вставьте в `IMAGE_URL` в модуле

### Способ 3: GitHub Raw

1. Загрузите изображение в GitHub репозиторий
2. Откройте файл → нажмите "Raw"
3. Скопируйте URL (начинается с `https://raw.githubusercontent.com/...`)
4. Вставьте в `IMAGE_URL`

**Примеры ссылок:**
```python
# Telegraph
IMAGE_URL = "https://te.legra.ph/file/abc123.jpg"

# GitHub
IMAGE_URL = "https://raw.githubusercontent.com/username/repo/main/image.jpg"

# Imgur
IMAGE_URL = "https://i.imgur.com/abc123.jpg"
```

## 🔧 Зависимости

- `pyrogram` / `kurigram` - основной фреймворк
- `psutil` (опционально) - для отображения использования памяти

Если `psutil` не установлен, вместо использования RAM будет отображаться "N/A".

## 📦 Установка зависимостей

```bash
pip install -r requirements.txt
```

## ⚙️ Технические детали

### Как работает uptime

Время старта сохраняется в `client.start_time` при запуске юзербота в `main.py`:

```python
await client.start()
client.start_time = time.time()
```

Модуль вычисляет разницу между текущим временем и временем старта.

### Получение информации о владельце

Используется `client.me` - объект, который доступен после `await client.start()`.

### Формат вывода

Используется HTML форматирование с blockquote и custom emoji:

```html
<blockquote>
<emoji id=5461117441612462242>🔥</emoji> Forelka Userbot
<blockquote>
...
</blockquote>
</blockquote>
```

## 🎨 Пример вывода

```
┌─────────────────────────┐
│ 🔥 Forelka Userbot      │
│ ┌─────────────────────┐ │
│ │                     │ │
│ │ 👤 Владелец: Иван С │ │
│ └─────────────────────┘ │
│                         │
│ ┌─────────────────────┐ │
│ │ 🌿 Branch: main     │ │
│ └─────────────────────┘ │
│                         │
│ ┌─────────────────────┐ │
│ │ ⚙️ Prefix: «.»      │ │
│ │ ⏱ Uptime: 2ч 15м 43с│ │
│ └─────────────────────┘ │
│                         │
│ ┌─────────────────────┐ │
│ │ 💾 RAM usage: 45.2 MB│ │
│ │ 🖥 Host: vps-server │ │
│ └─────────────────────┘ │
└─────────────────────────┘
```

## 🔄 Совместимость

Модуль полностью совместим с:
- ✅ Pyrogram
- ✅ Kurigram
- ✅ Любыми форками Pyrogram

Использует стандартный API и не зависит от специфичных функций.

## 🐛 Устранение проблем

### Модуль не загружается

Проверьте:
1. Файл `modules/info.py` существует
2. Синтаксис файла корректен: `python3 -m py_compile modules/info.py`
3. Модуль регистрируется: проверьте функцию `register()`

### RAM usage показывает "N/A"

Установите `psutil`:
```bash
pip install psutil
```

### Uptime всегда показывает 0 секунд

Убедитесь, что в `main.py` добавлена строка:
```python
client.start_time = time.time()
```

После `await client.start()` и перед `loader.load_all(client)`.

## 📝 Код модуля

Файл: `modules/info.py`

Основные функции:
- `info_cmd(client, message, args)` - основная функция команды
- `register(app, commands, module_name)` - регистрация команды

---

Создано для Forelka Userbot 👻
