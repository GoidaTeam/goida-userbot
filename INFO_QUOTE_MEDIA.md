# 🖼 Quote Media для модуля Info

Реализована функциональность Quote Media из MCUB - теперь `.info` поддерживает различные режимы отображения баннера!

## 🎯 Возможности

### 3 режима работы:

1. **Quote Media Mode** (Link Preview) - баннер как превью сверху
2. **Image Attachment Mode** - баннер как прикрепленное фото
3. **Text Only Mode** - только текст без баннера

## 📋 Команды

```bash
.info                              # Показать информацию
.setinfobanner [url]               # Установить URL баннера
.setinfobanner quote [on/off]      # Включить/выключить quote media
.setinfobanner clear               # Удалить все настройки баннера
.setinfobanner                     # Показать текущие настройки
```

## 🔧 Настройка

### 1. Установить баннер (Web URL для Telegraph)

```
.setinfobanner https://te.legra.ph/Forelka-Userbot-01-05
```

**Результат:**
```
✅ Баннер установлен!

Type: 🌐 Web URL
URL: https://te.legra.ph/Forelka-Userbot-01-05

💡 Tip: Используйте .setinfobanner quote on для включения quote media режима
```

### 2. Включить Quote Media режим

```
.setinfobanner quote on
```

**Результат:**
```
✅ Quote Media ✅ Включен
```

### 3. Проверить настройки

```
.setinfobanner
```

**Результат:**
```
👻 Info Banner Settings

Quote Media: ✅ Enabled
Banner URL: https://te.legra.ph/Forelka-Userbot-01-05

Команды:
.setinfobanner [url] - установить URL баннера
.setinfobanner quote [on/off] - включить/выключить quote media
.setinfobanner clear - удалить баннер
```

### 4. Использовать команду info

```
.info
```

Теперь `.info` покажет баннер как превью сверху!

## 🎨 Режимы работы

### Режим 1: Quote Media (Link Preview)

**Когда активен:**
- `info_quote_media = true`
- `info_banner_url` = веб URL (Telegraph, сайт с og:image)

**Как работает:**
```
┌─────────────────────┐
│  [ПРЕВЬЮ БАННЕРА]   │ ← Link Preview сверху
│   (от Telegraph)    │
└─────────────────────┘

🔥 Forelka Userbot    ← Текст снизу
👤 Владелец: ...
🌿 Branch: ...
⚙️ Prefix: ...
⏱ Uptime: ...
💾 RAM: ...
🖥 Host: ...
```

**Преимущества:**
- ✅ Красивый превью как на сайтах
- ✅ НЕ скачивает изображение
- ✅ Быстрая загрузка
- ✅ Изображение сверху, текст снизу

### Режим 2: Image Attachment (Фото)

**Когда активен:**
- `info_quote_media = false`
- `info_banner_url` = любой URL или локальный файл

**Как работает:**
```
┌─────────────────────┐
│  [ФОТО БАННЕРА]     │ ← Прикрепленное фото
└─────────────────────┘

🔥 Forelka Userbot    ← Caption под фото
👤 Владелец: ...
```

**Преимущества:**
- ✅ Работает с локальными файлами
- ✅ Работает с любыми URL
- ✅ Фото сверху, текст как caption

### Режим 3: Text Only (Только текст)

**Когда активен:**
- `info_banner_url` не установлен

**Как работает:**
```
🔥 Forelka Userbot
👤 Владелец: ...
🌿 Branch: ...
⚙️ Prefix: ...
⏱ Uptime: ...
💾 RAM: ...
🖥 Host: ...
```

**Преимущества:**
- ✅ Минимализм
- ✅ Быстро
- ✅ Не требует настройки

## 📖 Примеры использования

### Пример 1: Telegraph статья с изображением

```bash
# 1. Создайте статью на https://telegra.ph
# 2. Загрузите красивую картинку
# 3. Опубликуйте и скопируйте URL

# 4. Установите URL
.setinfobanner https://te.legra.ph/Forelka-Banner-01-05

# 5. Включите quote media
.setinfobanner quote on

# 6. Проверьте
.info
```

### Пример 2: Локальный файл

```bash
# 1. Загрузите изображение на сервер
# Например: /home/user/forelka-banner.jpg

# 2. Установите путь
.setinfobanner /home/user/forelka-banner.jpg

# 3. Quote media будет выключен автоматически (локальные файлы не поддерживают preview)

# 4. Проверьте
.info
# Фото будет отправлено как attachment
```

### Пример 3: Обычная ссылка на изображение

```bash
# 1. Загрузите изображение на Imgur или похожий хостинг
# 2. Скопируйте прямую ссылку

.setinfobanner https://i.imgur.com/abc123.jpg

# 3. Выключите quote media (прямые ссылки на jpg не дают preview)
.setinfobanner quote off

# 4. Проверьте
.info
# Фото будет скачано и отправлено
```

### Пример 4: Переключение режимов

```bash
# Текущий: quote media ON

# Временно выключить quote media (оставить URL)
.setinfobanner quote off
.info  # Фото как attachment

# Включить обратно
.setinfobanner quote on
.info  # Превью сверху
```

## 🔧 Настройки в config-{id}.json

Модуль автоматически сохраняет настройки в конфиг:

```json
{
  "prefix": ".",
  "info_quote_media": true,
  "info_banner_url": "https://te.legra.ph/Forelka-Userbot-01-05",
  "info_invert_media": false
}
```

### Параметры:

- **`info_quote_media`** (bool) - включить quote media режим
  - `true` = link preview (только для веб URL)
  - `false` = image attachment

- **`info_banner_url`** (string) - URL или путь к баннеру
  - Веб URL: `https://te.legra.ph/...`
  - Локальный файл: `/path/to/image.jpg`
  - Пустая строка: нет баннера

- **`info_invert_media`** (bool) - не используется в Pyrogram
  - Для совместимости с MCUB
  - Pyrogram не поддерживает invert_media

## 🌐 Поддержка форумов/тредов

Модуль автоматически поддерживает:
- ✅ Ответы в тредах (`reply_to_message_id`)
- ✅ Сообщения в топиках форумов (`message_thread_id`)
- ✅ Обычные чаты

## 🐛 Обработка ошибок

### Fallback сценарии:

1. **Quote media не работает** → отправит как фото
2. **Фото не загружается** → отправит только текст
3. **Неверный URL** → отправит только текст
4. **Баннер не найден** → отправит только текст

Модуль никогда не упадет - всегда будет fallback!

## 💡 Советы

### Для Quote Media режима:

1. **Используйте Telegraph**
   - Создайте статью с картинкой
   - Telegraph генерирует красивые превью
   - Быстро и надежно

2. **Не используйте прямые ссылки на .jpg**
   - `https://example.com/image.jpg` НЕ даст превью
   - Telegram показывает превью только для страниц
   - Используйте Telegraph или сайты с Open Graph

3. **Проверьте preview**
   - Откройте URL в Telegram (отправьте ссылку себе)
   - Если есть превью - значит будет работать
   - Если нет превью - используйте другой URL

### Для Image Attachment режима:

1. **Локальные файлы**
   - Используйте абсолютные пути
   - Проверьте права доступа
   - JPG, PNG, GIF поддерживаются

2. **Веб URL**
   - Можно использовать прямые ссылки на изображения
   - Imgur, Discord CDN, GitHub работают
   - Файл будет скачан и отправлен

## 🎯 Сравнение с MCUB_info.py

### Портированные функции:

✅ **Quote Media Mode** - link preview режим
✅ **Banner URL** - поддержка веб URL и локальных файлов
✅ **Forum/Thread Support** - reply_to и message_thread_id
✅ **Configuration** - чтение из config-{id}.json
✅ **Error Handling** - fallback на текст
✅ **Zero-width char** - `\u200d` для скрытия URL

### Адаптации для Pyrogram:

🔄 **Link Preview** - использует `disable_web_page_preview=False`
🔄 **Send Methods** - `send_message()` и `send_photo()`
🔄 **Config Loading** - json.load() вместо Telethon DB
❌ **Invert Media** - не поддерживается в Pyrogram (параметр игнорируется)

## 📚 Примеры конфигов

### Конфиг 1: Quote Media + Telegraph

```json
{
  "prefix": ".",
  "info_quote_media": true,
  "info_banner_url": "https://te.legra.ph/Forelka-Banner-01-05"
}
```

**Результат:** Превью сверху, текст снизу

### Конфиг 2: Image Attachment + Local File

```json
{
  "prefix": ".",
  "info_quote_media": false,
  "info_banner_url": "/home/user/banner.jpg"
}
```

**Результат:** Фото отправляется как attachment

### Конфиг 3: Text Only

```json
{
  "prefix": "."
}
```

**Результат:** Только текст, без баннера

---

**Готово! Теперь `.info` поддерживает Quote Media!** 🎨✨

Документация модуля: `INFO_MODULE.md`
